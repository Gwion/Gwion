// coverage for 'Slice'. (generated by util/coverage.sh)

Slice a;

// testing 'init'
{
	ftbl	vals;
	vals.init(1024);
	ftbl	buf;
	buf.init(1024);

	a.init(vals, buf);
}

// testing 'id'
{

	a.id();
}

// testing 'id'
{
	float	id;

	a.id(id);
}
a => dac;
second => now;
