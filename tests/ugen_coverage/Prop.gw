// coverage for 'Prop'. (generated by util/coverage.sh)

Prop a => dac;
samp => now;
a =< dac;

// testing 'init'
{
	"+ + 3(+ - +) 2(- 3(+ + +)) 3(+ +) + 4(+ +)" => string	str;

	a.init(str);a.init(str);
}

// testing 'bpm'
{

	a.bpm();
}

// testing 'bpm'
{
	float	bpm;

	a.bpm(bpm);
}
samp => now;
a=> dac;
second => now;
