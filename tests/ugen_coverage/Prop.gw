// coverage for 'Prop'. (generated by util/coverage.sh)

Prop a;

// testing 'init'
{
	"+ + 3(+ - +) 2(- 3(+ + +)) 3(+ +) + 4(+ +)" => string	str;

	a.init(str);
}

// testing 'bpm'
{

	a.bpm();
}

// testing 'bpm'
{
	float	bpm;

	a.bpm(bpm);
}
a => dac;
second => now;
