// coverage for 'Oscmorph'. (generated by util/coverage.sh)

Oscmorph a => dac;
samp => now;
a =< dac;

// testing 'init'
{
	ftbl	tbl[2];
	tbl[0].gen_sine(1024);
	tbl[1].gen_sine(1024);

	2 => int	nft;
	float	phase;

	a.init(tbl, nft, phase);a.init(tbl, nft, phase);
}

// testing 'freq'
{

	a.freq();
}

// testing 'freq'
{
	400 => float	freq;

	a.freq(freq);
}

// testing 'amp'
{

	a.amp();
}

// testing 'amp'
{
	.2 => float	amp;

	a.amp(amp);
}

// testing 'wtpos'
{

	a.wtpos();
}

// testing 'wtpos'
{
	float	wtpos;

	a.wtpos(wtpos);
}
samp => now;
a=> dac;
second => now;
