// coverage for 'SinOsc'. (generated by util/coverage.sh)

SinOsc a => dac;

// testing 'init'
{
	int	size;

	a.init(size);
	17 => size;

	a.init(size);
}

// testing 'init'
{
	int	size;
	float	phase;

	a.init(size, phase);
    12 => size;
	a.init(size, phase);
}

// testing 'freq'
{

	a.freq();
}

// testing 'freq'
{
	float	freq;

	a.freq(freq);
}

// testing 'amp'
{

	a.amp();
}

// testing 'amp'
{
	float	amp;

	a.amp(amp);
}

second => now;
