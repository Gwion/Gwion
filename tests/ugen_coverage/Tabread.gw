// coverage for 'Tabread'. (generated by util/coverage.sh)

Tabread a => dac;
samp => now;
a =< dac;

// testing 'init'
{
	ftbl	ft;
	ft.gen_sine(1024);(1024);
	float	mode;

	a.init(ft, mode);a.init(ft, mode);
}

// testing 'index'
{

	a.index();
}

// testing 'index'
{
	float	index;

	a.index(index);
}

// testing 'offset'
{

	a.offset();
}

// testing 'offset'
{
	float	offset;

	a.offset(offset);
}

// testing 'wrap'
{

	a.wrap();
}

// testing 'wrap'
{
	float	wrap;

	a.wrap(wrap);
}
samp => now;
a=> dac;
second => now;
