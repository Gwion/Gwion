// coverage for 'Pshift'. (generated by util/coverage.sh)

Pshift a => dac;
samp => now;
a =< dac;

// testing 'shift'
{

	a.shift();
}

// testing 'shift'
{
	float	shift;

	a.shift(shift);
}

// testing 'window'
{

	a.window();
}

// testing 'window'
{
	float	window;

	a.window(window);
}

// testing 'xfade'
{

	a.xfade();
}

// testing 'xfade'
{
	float	xfade;

	a.xfade(xfade);
}
samp => now;
a=> dac;
second => now;
