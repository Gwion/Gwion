#pragma memoize 3
fun int recursive_fib(int n) {
    if (n < 2)
        return n;
    else
        return recursive_fib(n - 2) + recursive_fib(n - 1);
}
<<< 1 => recursive_fib >>>;
<<< 1 => recursive_fib >>>;
#!<<< 12 => recursive_fib >>>;
<<< 40 => recursive_fib >>>;
