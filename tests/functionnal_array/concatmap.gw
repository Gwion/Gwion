
#!+ this is doc a comment

#!- this also

const float ii;
123=> var float f;

for(a : [12, 1, 2 ,3].compactMap:[int]( \a {
    var int?i;
    145 => i.val;
    if(maybe)
      i.val++;
    return i;
  }
)) {
  <<< "mapped to => ${a}" >>>;
}

fun int? t(int x) {
  var int?i;
  145 => i.val;
  if(maybe)
    i.val++;
  return i;
}
for(a : [12, 1, 2 ,3].compactMap:[int](t))
  <<< "mapped to => ${a}" >>>;
<<< "test" >>>;
