class C:[A] {
  \a{};
  [ 1 ];
  var int i,j;
  var typeof(i) k;
  'a';
  true $ int;
  i++;
  i ? i : !i;
  ++i;
  union U { int }
  union V:[A] { int }
  typeof(i);
  if(i) i; else i;
  for(var int _i; _i < 1; ++_i);
  var int array[2];
  array[0];
  foreach(a : array);
  repeat(1);
  match i,2 {
    case 12 when 0:;
    case _,2 when 0:;
  } where var int b;
  enum { zero };
  typedef int Int;
  goto end;
  new Object;
  spork { <<< __func__ >>>; };
  end:
  while(0){ <<< __func__ >>>; }
}

var C:[int] c;
