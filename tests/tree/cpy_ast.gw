class C {
  \a{};
  [ 1 ];
  int i,j;
  `i` k;
  @(0);
  'a';
  true $ int;
  i++;
  i ? i : !i;
  ++i;
  union { int ui; };
  typeof(i);
  if(i) i; else i;
  for(int; i < 1; ++i);
  int array[2];
  for(auto a : array);
  repeat(1);
#! gotos ...
  match i { case _:; }
  enum { zero };
  typedef int Int;
}
C c;
