# While Loops

```gwion,editable
while(true) {
  if(maybe)
    break;
  <<< "running..." >>>;
}
```  

well this may output nothing...
lets try

```gwion,editable
<<< maybe >>>;
do{
  if(maybe)
    break;
  <<< "running..." >>>;
} while(true);
```  
