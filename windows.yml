name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name : submodule
      run: git checkout submodule update --init util ast
    - name: make
      run: make
